{% extends 'base.html.twig' %}

{% block body %}
<div class="container">
    {{ form_start(form) }}
        {{ form_row(form.title) }}
        <button type="submit" id="btn">Submit</button>
    {{ form_end(form) }}
</div>
    {% endblock %}
